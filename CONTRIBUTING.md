# 贡献与实验记录规范

- 新实验放在对应方法目录，不把训练输出、缓存和模型权重提交到 Git。
- 每个可比较实验至少记录：数据路径、随机种子、关键超参数、指标定义和运行命令。
- 可复现脚本使用相对项目根目录的路径，避免写入个人电脑的绝对路径。
- 图表放在对应目录的 `Figure/`，并在 README 或 Log 中注明生成脚本和数据来源。
- 提交前运行 `git status --short`，确认没有 `private_materials/`、个人信息文档和本地临时文件。
