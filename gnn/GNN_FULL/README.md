# GNN multi-task archive

`GNN_FULL/` 保存早期共享主干的分类+回归多任务实验。它曾尝试让一个网络同时输出变化数量和 112 维变化量，但当前正式联合推理已经拆分为 `GNN_CLS` 与 `GNN_REG` 两条线。

这里的代码适合做多任务对照，不是当前默认入口。复跑前请检查模型权重、数据标签和输出路径；详细历史见 [`../Log.md`](../Log.md)。
