# 正方形规模研究日志

## 说明

本文件用于记录 `square_scale_study` 的实际实验过程、阶段结论和后续交接信息。

- `PLAN.md` 负责固定问题定义、指标和实验规则
- `Log.md` 负责记录已经做过的事、观察到的现象和当前结论

建议每一轮实验至少记录：

- 日期
- 规模范围与 `K` 范围
- 数据规模
- 激励设置
- 实际运行内容
- 核心观察
- 下一步动作

## 2026-04-10：子项目初始化

- 新建 `square_scale_study/` 子项目，与旧 `gnn` 主线解耦
- 搭建固定 `K` 数据生成、最简回归训练、推理导出、规模扫描和多激励入口
- 清理初始化时产生的 smoke 产物，避免正式实验前目录混乱

## 2026-04-10：正式数据批次

- 新增 `generate_dataset_grid.py`，支持批量生成 `(N, K)` 数据集
- 完成 `N = 3, 4, 5`、`K = 1..6` 的正式数据集生成
- 每个 `(N, K)` 使用统一数据规模：
  - train: `8000`
  - val: `1000`
  - test: `1000`
- 数据为直接解基尔霍夫方程得到的无噪声正演结果
- 默认激励方式为顺时针边界相邻循环激励，激励数等于端口数 `P`

## 2026-04-11：`modelv1` 全量基线

### 运行范围

- 规模：`N = 3, 4, 5`
- 变化数量：`K = 1..6`
- 模型：`modelv1`
- 目标：建立固定 `K` 下的最简纯回归基线

### 关键结果

- `P = 8 (N=3)`：`K_max = 1`
- `P = 12 (N=4)`：`K_max = 1`
- `P = 16 (N=5)`：在严格 `id >= 0.98` 条件下，`K_max = 0`

主结果说明：

- `value_accuracy` 在大多数点上仍维持较高水平
- 真正的瓶颈是 `id_exact_rate`，且会随着 `K` 增大和规模增大明显下降
- 说明 `modelv1` 更像一个“数值回归基线”，而不是最终可用方案

### 关键误差分析

首个失败点分析结果：

- `N3x3, K=2`：
  - `id_exact_rate = 0.938`
  - `value_accuracy = 0.9748`
  - `mean_true_rank = 1.643`
  - `near_miss_rate_on_failures = 0.323`
- `N4x4, K=2`：
  - `id_exact_rate = 0.892`
  - `value_accuracy = 0.9604`
  - `mean_true_rank = 1.939`
  - `near_miss_rate_on_failures = 0.389`
- `N5x5, K=1`：
  - `id_exact_rate = 0.975`
  - `value_accuracy = 0.9643`
  - `mean_true_rank = 1.325`
  - `near_miss_rate_on_failures = 0.120`

结论：

- `modelv1` 失败的主要原因不是数值回归崩坏，而是 support 排序不够稳定
- 真实变化边通常已经排在较靠前位置，但仍会被少量假阳性边挤出 top-`K`
- 后续模型修改应优先围绕 support 恢复而不是单纯继续压低回归误差

### 记录归档

- `0410modelv1日志.txt` 的重要内容已由 `outputs/`、汇总表和本日志归档
- 后续不再保留该长日志文本

## 2026-04-11：`modelv2` 全量结果

### 运行范围

- 规模：`N = 3, 4, 5`
- 变化数量：`K = 1..6`
- 模型：`modelv2`
- 结构：score head + value head
- 损失：ranking loss + 带变化边/未变化边权重的两部分 SmoothL1

### 关键结果

- `P = 8 (N=3)`：`K_max = 1`
- `P = 12 (N=4)`：`K_max = 0`
- `P = 16 (N=5)`：`K_max = 1`

与 `modelv1` 对比后的整体结论：

- `modelv2` 在多数点上进一步提高了 `value_accuracy`
- 但 `id_exact_rate` 并没有整体改善，很多点反而更低
- 因而它并没有把主线的 `K_max` 曲线整体抬起来

### 关键误差分析

首个关键失败点分析结果：

- `N3x3, K=2`：
  - `id_exact_rate = 0.943`
  - `value_accuracy = 0.9833`
  - `mean_true_rank = 1.583`
  - `near_miss_rate_on_failures = 0.579`
- `N4x4, K=1`：
  - `id_exact_rate = 0.978`
  - `value_accuracy = 0.9705`
  - `mean_true_rank = 1.157`
  - `near_miss_rate_on_failures = 0.409`
- `N5x5, K=2`：
  - `id_exact_rate = 0.809`
  - `value_accuracy = 0.9622`
  - `mean_true_rank = 2.087`
  - `near_miss_rate_on_failures = 0.497`

结论：

- `score head + ranking loss` 并没有自动转化为更高的样本级精确 support 恢复率
- 当前 `modelv2` 更像是“数值更稳、排序仍不够强”的版本
- 这意味着继续直接增加复杂度不一定能解决问题，下一步应优先考虑更轻量、更聚焦的结构修改

### 记录归档

- `0411modelv2日志.txt` 的重要内容已由 `outputs_modelv2/`、汇总图表和本日志归档
- 后续不再保留该长日志文本

## 当前下一步

- 后续模型改进优先在 `3x3` 网络上快速迭代
- 只有当 `3x3` 上出现明显提升后，才扩展到更大规模
- 下一轮应优先尝试更简单、更直接对准 support 恢复的结构，而不是继续堆深层注意力模块

## 2026-04-11：`modelv3` 首轮 `3x3` 小规模验证

### 运行范围

- 规模固定为 `N = 3`
- 变化数量测试为 `K = 2, 3, 4`
- 模型为 `modelv3`
- 输出目录为 `outputs_modelv3/`
- 图表目录为 `Figure/metric_dropoff_by_port_modelv3/`
- 目标是验证“更轻量、更浅层、直接面向 support 恢复”的 `modelv3` 是否优于 `modelv1/modelv2`

### 模型设置

- 结构采用单层浅层 GATv2 主干，`hidden_dim = 64`
- 不再使用较重的多层注意力堆叠，也不做跨激励注意力
- 跨激励融合改为 `mean + max` 的简单聚合
- 保留 `score head + value head`
- 损失为：
  - `score BCE`
  - `ranking hinge loss`
  - 带 changed / unchanged 权重的两部分 `SmoothL1`

### 关键测试结果

- `N3x3, K=2`
  - `test_id_exact_rate = 0.925`
  - `test_value_accuracy = 0.9823`
  - `test_mae_changed = 3.5486`
- `N3x3, K=3`
  - `test_id_exact_rate = 0.810`
  - `test_value_accuracy = 0.9779`
  - `test_mae_changed = 4.4226`
- `N3x3, K=4`
  - `test_id_exact_rate = 0.668`
  - `test_value_accuracy = 0.9701`
  - `test_mae_changed = 5.9830`

最佳验证轮次：

- `K=2`：`best_epoch = 50`
- `K=3`：`best_epoch = 46`
- `K=4`：`best_epoch = 47`

### 与前两版对比

同样只看 `N=3` 上的测试集结果：

- `K=2`
  - `modelv1`: `id = 0.938`, `value = 0.9748`
  - `modelv2`: `id = 0.943`, `value = 0.9833`
  - `modelv3`: `id = 0.925`, `value = 0.9823`
- `K=3`
  - `modelv1`: `id = 0.884`, `value = 0.9702`
  - `modelv2`: `id = 0.852`, `value = 0.9823`
  - `modelv3`: `id = 0.810`, `value = 0.9779`
- `K=4`
  - `modelv1`: `id = 0.810`, `value = 0.9707`
  - `modelv2`: `id = 0.778`, `value = 0.9789`
  - `modelv3`: `id = 0.668`, `value = 0.9701`

结论：

- `modelv3` 没有在 `3x3` 上超过 `modelv1` 或 `modelv2`
- 尤其在 `K=3,4` 时，`id_exact_rate` 明显落后
- 这说明“简单化结构”本身并不会自动带来更好的 exact support recovery

### 误差分析

- `K=2`
  - `mean_true_rank = 1.623`
  - `near_miss_rate_on_failures = 0.573`
- `K=3`
  - `mean_true_rank = 2.206`
  - `near_miss_rate_on_failures = 0.516`
- `K=4`
  - `mean_true_rank = 2.753`
  - `near_miss_rate_on_failures = 0.491`

可以看到：

- 随着 `K` 增大，真实变化边在排序中的平均名次持续变差
- 失败样本里约有一半仍属于“邻近边/近似边混淆”
- 主瓶颈依旧不是回归值本身，而是 top-`K` support 排序不够稳

### 本轮结论

- `modelv3` 证明了一个重要事实：当前问题的核心矛盾不在“主干太重”，而在“训练目标没有真正把 exact support 恢复推起来”
- 仅靠浅层化 + 双头 + ranking loss，仍然不能把排序优势稳定转化为样本级 `id_exact_rate`
- 因而下一步不应继续沿着“更深或更浅的 GAT 结构微调”反复试，而应直接修改支持集恢复机制

### 下一步重点

- 继续只在 `3x3` 上做快速迭代，不扩规模
- 保留 `K=1..4` 的小实验矩阵，先把 `K=2,3` 的 `id_exact_rate` 做出实质提升
- 下一轮模型应优先探索：
  - 更直接的 support 监督方式
  - 更贴近 top-`K` 选边过程的训练目标
  - 明确区分“选边”与“数值回归”的两阶段机制

### 结果解释备注

- 当前 `outputs_modelv3/port_vs_kmax_summary.csv` 中显示的 `K_max = 0`，仅仅是因为这一轮只跑了 `K = 2, 3, 4`
- 该汇总表缺少 `K = 1` 的结果，因此不能被解释为“`modelv3` 在 `K=1` 也失败”
- 若后续需要正式比较 `K_max`，必须补跑 `N3x3, K=1`

## 2026-04-12：关于“物理识别上限”判据的补充

### 可采纳的核心观点

- 当前问题应明确区分两类瓶颈：
  - 算法瓶颈：模型没有学好
  - 物理瓶颈：边界测量本身不包含足够信息
- 在无噪声条件下，单纯继续比较不同神经网络精度并不能直接回答“是否达到该规模下的物理识别上限”
- 更合理的思路是增加一套“极限可拟合性”测试，用来判断失败到底来自模型能力不足，还是来自信息本身不可分

### 需要修正的地方

- 不能简单把“训练集也过拟合不上”直接等同于“已经达到物理极限”
- 这只能说明：在当前模型族、当前优化方式和当前训练目标下，系统已经很难再被拟合
- 若要更接近“物理极限”结论，还需要同时满足：
  - 已经使用了更宽松、明显偏向记忆能力的无噪声测试设置
  - 多种模型或求解方式都失败
  - 物理侧分析也显示有效信息维度受限或样本间存在强歧义

### 当前项目下更合适的判断标准

对于固定规模 `N` 和固定变化数量 `K`，建议建立一套“物理上限判据”：

1. 在无噪声条件下，使用专门的 limit-test 设置，而不是直接沿用当前偏稳健的主线模型
2. 去掉或显著减弱不必要的保守项，例如较强的 dropout、过强的排序约束、过强的未变化边压制
3. 允许模型以“训练集能否被记住”为目标，而不是以泛化为首要目标
4. 如果在这种放宽条件下，训练集 support 仍长期无法接近 100%，再把它视为“接近物理极限”的强信号

### 建议的验证流程

- 第一步：做一个专门的 `limit-test` 版本
  - 固定无噪声
  - 固定单一 `N`
  - 固定单一 `K`
  - 去掉不必要的稳健化设计
- 第二步：先只看训练集拟合能力
  - 如果训练集都记不住，说明问题首先不是泛化，而是信息与表示本身不足
- 第三步：再结合物理侧指标
  - 响应矩阵有效秩
  - 奇异值谱衰减
  - 样本间响应最小间隔
  - 是否存在大量近重复响应
- 第四步：只有当“算法侧记不住”与“物理侧确实低可分”同时出现时，才汇报为“接近该规模下的物理识别上限”

### 对当前工作的指导意义

- Gemini 的分析方向整体是有道理的，尤其是“不要把所有失败都归因于模型不够强”这一点非常重要
- 但在我们的项目里，下一步不是直接宣称已经到达物理极限，而是先新增一条更干净的无噪声 `limit-test` 支线
- 这条支线的目标不是发表最终结果，而是回答一个更基础的问题：
  - 当前失败究竟主要来自 support 学习机制，还是来自边界测量的信息天花板

## 2026-04-12：外部模型分析意见的吸收与修正

### 总体判断

- Gemini 与 Deepseek 的外部分析，方向上提供了两个有价值的提醒：
  - 不要把当前失败全部理解成“模型还不够大”
  - 在无噪声条件下，应专门区分“同规模泛化目标”与“物理上限诊断目标”
- 这与当前项目的新主线是相容的
- 但两份外部分析里也夹杂了一些对现有 `modelv3` 代码的错误判断，因此只能把它们当作思路参考，不能直接当作代码诊断结论

### 可以吸收的经验

- 对当前课题而言，可以放弃“跨规模泛化”作为近期目标
- 更合理的目标是：
  - 固定一个规模 `N`
  - 只研究该规模内部、随机样本之间的识别能力
- 因而保留 `8:1:1` 的 train / val / test 划分仍然是有意义的：
  - train：检验模型是否能真正学会该规模下的映射
  - val：选模型、看是否稳定
  - test：看同规模随机样本上的泛化能力
- 在无噪声条件下，应单独增加一条高容量 `limit-test` 支线，专门判断“当前失败更像算法瓶颈还是信息瓶颈”
- 若后续 GNN 与 MLP 两类大模型都在同一个 `(N, K)` 上训练集也无法逼近完美拟合，那么“接近物理上限”的判断才会更有说服力

### 需要修正的地方

- 外部分析中提到的某些具体代码问题并不适用于当前仓库中的 `modelv3`
- 例如：
  - 并不存在它所说的固定 `112` 条边的硬编码 edge embedding
  - 当前 `modelv3` 也不存在它提到的那种 `loss_sparse` 项
- 因此，外部分析里关于“某行代码导致当前模型失败”的结论不能直接采纳
- 另外，对 `N=3` 而言，盲目把 GNN 叠到很多层并不一定更好，因为图直径本来就小，过深更容易出现过平滑
- 在 `N=3` 上，更合理的是：
  - 宽度做大
  - 残差做强
  - 跳连做清楚
  - 支持集监督做直接

### 对当前主线的重新定位

- 当前主线不再追求“一套模型跨多个规模都通用”
- 当前主线改为：
  - 先固定 `N=3`
  - 在 `N=3` 内部把 `K=1..4` 的识别问题做扎实
  - 等到 `N=3` 上结果稳定后，再迁移到更大规模

## 2026-04-12：`limit-test` 的目的与方法说明

### 目的

`limit-test` 不是新的最终模型路线，而是一条诊断路线。

它要回答的问题是：

- 某个固定 `(N, K)` 失败，到底是因为当前模型没学会
- 还是因为边界电压里本来就没有足够信息来区分这些变化模式

### 核心思想

- 主线模型关心的是“同规模下能否稳健泛化”
- `limit-test` 关心的是“在明显放宽限制、明显提高容量后，训练集还能不能被学会”

如果一个高容量模型在无噪声、弱正则、固定规模、固定 `K` 的条件下，训练集仍然长期无法逼近几乎完美的 support 恢复，那么这才是“接近物理上限”的强信号

### 推荐做法

- 数据仍保留 `8:1:1`
- 但在 `limit-test` 中，训练集指标是第一优先级
- 同时保留 val / test，是为了防止把单次优化失败误认为物理极限

判断逻辑建议如下：

1. 若 train 很高，val / test 也高：该 `(N, K)` 明显可识别
2. 若 train 很高，但 val / test 明显差：说明信息足够，但模型只会记忆，泛化还没做好
3. 若 train 也始终上不去，且 GNN / MLP 两条线都失败：说明该 `(N, K)` 很可能已经接近物理识别上限

### 当前阶段的使用方式

- `limit-test` 先只在 `N=3` 上做
- 只测 `K=2, 3, 4`
- 不直接拿来汇报“最终最优模型”
- 它的作用是帮我们先判断：
  - 是该换训练目标
  - 还是已经撞到了信息天花板

## 2026-04-12：下一版双路线详细计划

### 共同原则

- 近期不研究跨规模泛化
- 近期只研究固定规模内部的随机样本泛化
- 保留 `8:1:1` 划分
- 先固定 `N=3`
- 默认测试 `K = 1, 2, 3, 4`
- 每条路线都保留三组指标：
  - train
  - val
  - test
- 重点先看：
  - `id_exact_rate`
  - `value_accuracy`
  - train 是否能逼近满分

### 路线 A：大容量 GNN

目标：

- 在保留图结构先验的前提下，做一个真正高容量、但适合 `N=3` 的 support-first GNN

结构建议：

- 输入仍沿用当前 excitation-conditioned 图表示
- 节点编码器宽度提升到 `128` 或 `256`
- 图主干采用 `3~4` 层残差图块，不建议在 `N=3` 上盲目堆到更深
- 每层后加入：
  - residual skip
  - layer norm 或 batch norm
  - GELU / LeakyReLU
- 采用 jump knowledge 或多层特征拼接，避免只用最后一层表示
- 跨激励融合不建议再做太复杂的注意力，优先试：
  - mean + max
  - 或轻量 attention pooling
- 输出继续拆成两头：
  - support score head
  - value regression head

训练建议：

- 第一阶段先把 support 头训稳：
  - BCE / focal 类损失
  - top-`K` ranking / margin loss
- 第二阶段再做联合训练：
  - support loss
  - changed-edge value loss
  - unchanged-edge 小权重 value loss
- 对 `N=3` 的主实验，建议默认关闭 dropout 或只保留极小 dropout
- `weight_decay` 设得很小
- 每个 `(K)` 至少跑 `3` 个 seed

这一条线的判断目标：

- 如果大容量 GNN 仍然在 `N=3, K=2/3` 上 train 都上不去，就说明问题不只是“主干太浅”

### 路线 B：大容量残差 MLP

目标：

- 去掉图网络的归纳偏置，直接测试“边界电压向量本身是否足够支持学习”
- 它同时也是对 GNN 结论的一个重要对照

结构建议：

- 输入直接展平为固定长度向量
- 做深宽残差 MLP：
  - 宽度 `1024` 或 `2048`
  - 深度 `6~8` 个残差块
  - 每块包含：
    - Linear
    - LayerNorm
    - GELU
    - Linear
    - residual
- 输出维度直接对应全部边
- 同样使用双头：
  - support head
  - value head

训练建议：

- 仍保留 `8:1:1`
- 同样优先看 train 指标，但必须同时记录 val / test
- 主损失与 GNN 线尽量统一，便于横向比较：
  - support BCE / focal
  - ranking loss
  - weighted value regression
- 默认也做 `3` 个 seed

这一条线的判断目标：

- 如果 MLP 能把 train 拟合得更高，而 GNN 不行，说明图结构实现或图归纳偏置可能限制了效果
- 如果 GNN 和 MLP 都卡在同一个 `(N, K)`，则更接近“信息本身受限”的解释

### 实验推进顺序

建议按这个顺序推进：

1. 先做 `N=3, K=2`
2. 若明显优于当前 `modelv3`，再测 `K=3`
3. 若 `K=3` 仍有进步，再测 `K=4`
4. 只有当 `N=3` 路线稳定后，再考虑上 `N=4`

### 当前最推荐的执行方式

- 不要先急着二选一
- 最合理的是：
  - GNN 作为主线
  - MLP 作为对照线
- 因为这能最快回答两个问题：
  - 图结构先验到底有没有帮上忙
  - 当前失败到底更像模型问题，还是信息问题
## 2026-04-12：modelo1 系列模型已落地

本轮已新增三条正式模型线，并统一记录到 `MODEL_DESIGN.md`：

- `modelo1_gnn`：大容量 support-aware GNN 主线
- `modelo1_mlp1`：带 score head 的高容量残差 MLP 对照线
- `modelo1_mlp2`：仅用纯 MSE 的高容量残差 MLP 对照线

本轮记录重点包括：

- 三个模型的功能分工
- 不同结果组合的解释方式
- 每个模型的架构与默认超参数
- 各模型 loss 的精确定义
- `N=3, K=2~4` 的推荐训练与推理命令
## 2026-04-13：N=3 上 modelo1 系列结果整理

本轮在固定 `N=3` 下，完成了四条 `modelo1` 线路的完整对照：

- `modelo1_gnn`
- `modelo1_gnn_mse`
- `modelo1_mlp1`
- `modelo1_mlp2`

统一测试范围为：

- `K = 2, 3, 4`
- 无噪声
- 固定 `8:1:1` 的 `train / val / test`

### 一、测试集结果总表

`modelo1_gnn`

- `K=2`：`test_id_exact_rate = 0.932`，`test_value_accuracy = 0.9876`
- `K=3`：`test_id_exact_rate = 0.893`，`test_value_accuracy = 0.9906`
- `K=4`：`test_id_exact_rate = 0.842`，`test_value_accuracy = 0.9901`

`modelo1_gnn_mse`

- `K=2`：`test_id_exact_rate = 0.977`，`test_value_accuracy = 0.9920`
- `K=3`：`test_id_exact_rate = 0.941`，`test_value_accuracy = 0.9915`
- `K=4`：`test_id_exact_rate = 0.905`，`test_value_accuracy = 0.9860`

`modelo1_mlp1`

- `K=2`：`test_id_exact_rate = 0.941`，`test_value_accuracy = 0.9848`
- `K=3`：`test_id_exact_rate = 0.873`，`test_value_accuracy = 0.9866`
- `K=4`：`test_id_exact_rate = 0.793`，`test_value_accuracy = 0.9848`

`modelo1_mlp2`

- `K=2`：`test_id_exact_rate = 0.979`，`test_value_accuracy = 0.9880`
- `K=3`：`test_id_exact_rate = 0.936`，`test_value_accuracy = 0.9884`
- `K=4`：`test_id_exact_rate = 0.889`，`test_value_accuracy = 0.9871`

### 二、与上一轮 `modelv3` 的对比

`modelv3` 的对应测试集结果为：

- `K=2`：`ID = 0.925`，`Value = 0.9823`
- `K=3`：`ID = 0.810`，`Value = 0.9779`
- `K=4`：`ID = 0.668`，`Value = 0.9701`

对比可见：

- 四条 `modelo1` 新线都整体优于 `modelv3`
- 提升最明显的是 `K=3,4` 时的 support 识别能力
- 尤其是新增的 `modelo1_gnn_mse`，把 GNN 线在 `K=3,4` 上的结果明显抬高了
- 说明这轮“先固定 `N=3`，再做结构与 loss 的正交对照”方向是有效的

### 三、当前最重要的实验结论

1. 当前最重要的结论不是“MLP 一定比 GNN 好”，而是“纯 `MSE` 版本整体优于 support-aware 版本”

- 在 MLP 组中：
  - `modelo1_mlp2 > modelo1_mlp1`
- 在 GNN 组中：
  - `modelo1_gnn_mse > modelo1_gnn`
- 这说明当前 `N=3` 设置下，`score head + ranking loss` 更像是在干扰最终的 top-`K` 恢复，而不是帮助它

2. 在加入 `modelo1_gnn_mse` 之后，GNN 线的判断发生了更新

- 之前如果只看 `modelo1_gnn`，会得出“GNN 不如 MLP”的结论
- 但加入同容量纯 `MSE` 的 `modelo1_gnn_mse` 后可以看到：
  - `K=2`：`modelo1_mlp2 = 0.979`，`modelo1_gnn_mse = 0.977`
  - `K=3`：`modelo1_gnn_mse = 0.941`，`modelo1_mlp2 = 0.936`
  - `K=4`：`modelo1_gnn_mse = 0.905`，`modelo1_mlp2 = 0.889`
- 这说明图结构先验并不是无效，而是**在不叠加额外 score/ranking 约束时，GNN 反而在更难的 `K=3,4` 上表现更强**

3. 当前瓶颈主要仍然体现在 `ID`，而不是数值回归

- 五条模型在 `K=2,3,4` 上的 `value_accuracy` 都已经稳定在 `0.982+`
- 真正拉开差距的是 `id_exact_rate`
- 说明当前问题依然不是“数值回归学不会”，而是“如何让 top-`K` support 排序更稳”

4. 当前严格阈值下，`N=3` 仍未正式通过

- 项目当前通过标准是：
  - `id_exact_rate >= 0.98`
  - `value_accuracy >= 0.90`
- 本轮最佳结果是 `modelo1_mlp2, K=2` 的 `id_exact_rate = 0.979`
- 它已经非常接近阈值，但在当前严格定义下仍然记为 `pass = False`

### 四、对后续工作的直接指导

当前阶段的最优先级结论是：

- `N=3` 下不应继续优先堆复杂的 score/ranking 设计
- 当前最值得继续推进的是两条纯 `MSE` 主线：
  - `modelo1_gnn_mse`
  - `modelo1_mlp2`
- 如果目标是继续冲击 `id_exact_rate >= 0.98`，那么当前最合理的做法不是再加复杂监督，而是围绕纯回归主线继续优化容量、正则和训练稳定性
- 下一步需要认真讨论的是：
  - `id_exact_rate` 的阈值是否必须固定为 `0.98`
  - 对每一个 `N`，是否都应单独寻找最合适的模型结构
  - 若目标是“估计该规模的最大可识别 `K`”，那么是否需要针对每个 `N` 先做充分的 per-`N` 模型优化，再宣布该 `N` 的 `K_max`

### 五、当前一句话总结

在固定 `N=3` 的完整五线对照中，新增的 `modelo1_gnn_mse` 证明了“GNN 并不一定弱，真正的问题更可能是 support-aware loss 干扰了学习”；当前最强路线已经从“更复杂的监督”转向“更干净的纯 `MSE` 回归”，其中 `K=2` 最优为 `modelo1_mlp2`，而 `K=3,4` 最优为 `modelo1_gnn_mse`。

## 2026-04-14：`modelo2` 版本落地与 `N=6~10` 数据补齐

### 一、本轮实现内容

- 新增两条面向更大规模的纯回归模型线：
  - `models/modelo2_gnn/`
  - `models/modelo2_mlp/`
- 新增批量数据生成脚本：
  - `scripts/generate_square_fixedk_range.py`
- 删除临时建议文件：
  - `square_scale_study/GNN扩大规模建议.txt`

### 二、`modelo2_gnn` 的设计原则

本轮参考了“不要盲目继续加深，而应优先扩大容量并保留 JK”的建议，具体落地为：

- 在 `modelo1_gnn_mse` 的纯 `MSE` 口径上继续扩容
- 默认 `hidden_dim = 384`
- 默认 `edge_hidden = 1024`
- 默认层数为 `6`
- 保留并升级 JK 为 attention-based JK
- 在原有相邻消息边基础上加入二阶消息边，以较浅层数覆盖更大感受野
- 引入静态节点/边拓扑特征编码，帮助模型区分更大规模下的位置与结构角色

### 三、`modelo2_mlp` 的设计原则

本轮 `modelo2_mlp` 继续沿着高容量纯回归路线推进，具体落地为：

- 默认 `hidden_dim = 3072`
- 默认 `num_blocks = 12`
- 默认 `ff_multiplier = 3.0`
- 继续使用深残差结构
- 把普通前馈块升级为 `GEGLU` 风格门控前馈
- 训练与推理口径仍然保持纯 `MSE`

### 四、`N=6~10` 数据补齐结果

已完成以下全部正式数据集的生成：

- `N = 6, 7, 8, 9, 10`
- 每个 `N` 下：
  - `K = 1, 2, 3, 4, 5, 6`
- 总计补齐 `30` 组 `(N, K)` 数据集

每组数据集均保持统一口径：

- 总样本数：`10000`
  - train: `8000`
  - val: `1000`
  - test: `1000`
- 只使用边缘节点作为激励端口
- 只测量边缘节点电压
- 每个样本固定 `K` 条变化电阻
- 电阻变化范围：`±20%`
- 正演方法：直接构建电导矩阵并直接求解基尔霍夫方程组

### 五、抽查结果

以 `N=10, K=6` 为例：

- `num_nodes = 100`
- `num_resistors = 180`
- `port_count = 36`
- `meta.json` 中已保存：
  - 边界节点顺序
  - 电阻边列表
  - 激励集合
  - 基线边界电压
  - 数据划分与生成参数
- `train.csv` 的一行对应一个激励下的一次边界响应展开结果，包含：
  - 样本 id
  - 激励 id
  - source / ground 节点
  - 所有边界节点电压
  - 固定 `K`
  - `K` 条变化电阻的 `id/value`

### 六、当前直接结论

- 当前主比较对象已经更明确：后续优先比较 `modelo2_gnn` 与 `modelo2_mlp`
- 由于 `modelo1` 阶段已经显示纯 `MSE` 路线更稳，这一轮 `modelo2` 刻意继续保持纯回归口径，避免重新引入额外 loss 干扰
- 数据侧已经不再缺 `6x6~10x10` 的正式样本，后续可以直接进入更大规模训练与多阈值分析

## 2026-04-15：`modelo2` 阶段结果并回项目日志

### 一、记录方式修正

- 前一轮曾临时把 `modelo2` 阶段总结单独写成了 `modelo2_progress_record.md`
- 这样做的初衷是把“阶段性训练结果”和“正式主线结论”分开，避免与已有 `modelv1/modelv2/modelv3` 记录混在一起
- 但从本项目的文档习惯看，`modelo2` 仍属于 `square_scale_study` 的正式推进过程，更合适的做法仍然是并回 `Log.md`
- 因此本次已经把关键结论并回本日志，后续不再保留独立的 `modelo2_progress_record.md`

### 二、阶段结果摘要

本轮 `modelo2` 的实际推进状态是：

- `modelo2_mlp`
  - `N=3, K=1~6`：完整训练完成
  - `N=6, K=1~6`：完整训练完成
  - `N=9, K=1`：只推进到日志阶段，已确认明显退化
- `modelo2_gnn`
  - `N=9, K=1`：在减小 batch 与 excitation chunk 后成功启动，但目前只记录到前若干 epoch

以“最佳验证集指标”口径整理后的关键结果为：

- `modelo2_mlp, N=3`
  - `K=1: val_id = 0.989`
  - `K=2: val_id = 0.968`
  - `K=3: val_id = 0.924`
  - `K=4: val_id = 0.885`
  - `K=5: val_id = 0.888`
  - `K=6: val_id = 0.877`
- `modelo2_mlp, N=6`
  - `K=1: val_id = 0.981`
  - `K=2: val_id = 0.769`
  - `K=3: val_id = 0.564`
  - `K=4: val_id = 0.334`
  - `K=5: val_id = 0.170`
  - `K=6: val_id = 0.090`
- `N=9, K=1` 的阶段性对比
  - `modelo2_mlp`：best `val_id = 0.011`
  - `modelo2_gnn`：当前 best `val_id = 0.394`

### 三、阶段结论

- `modelo2_mlp` 在小规模 `N=3` 上仍然能工作，但到 `N=6` 以后，随着 `K` 增加，support 识别能力出现明显断崖
- `modelo2_mlp` 在 `N=9, K=1` 上已接近退化为“全零预测器”，继续沿超大纯 `MLP` 方向投入的价值很低
- `modelo2_gnn` 在同一 `N=9, K=1` 上虽然仍远未达标，但明显优于 `modelo2_mlp`，说明大规模下图结构先验仍然有价值
- 当前真正的问题不是“GNN 完全无效”，而是 `modelo2_gnn` 计算代价过高、训练过慢，不适合作为后续大批量 sweep 的主力模型

### 四、产物位置

本轮 `modelo2` 阶段图表统一保存在：

- `Figure/modelo2_progress/`

其中包括：

- `modelo2_mlp_bestval_summary.csv`
- `modelo2_mlp_bestval_table.png`
- `modelo2_mlp_scale_compare.png`
- `N3_P8_bestval_dropoff.png`
- `N6_P20_bestval_dropoff.png`
- `modelo2_n9_k1_training_compare.png`
- `modelo2_partial_log_summary.csv`

## 2026-04-16：与师兄对齐后的三项新主线

### 一、最终需要产出的三幅主图

后续汇报的核心可视化目标正式固定为三张图：

1. `3x3 / 4x4 / 5x5 / 6x6` 的 `端口数 P - 最大可识别变化数量 K_max` 曲线  
   这张图延续原始主线，但只保留中小规模四个点，先把尺度规律讲清楚
2. `6x6` 下的 `可变电阻总数 M_var - K_max` 曲线  
   固定全部外部端口都可用，研究“允许变化的候选电阻总数变多”是否会压低可识别上限
3. `6x6` 下的 `可测端口数 P_meas - K_max` 曲线  
   固定全部电阻都仍可变化，研究“仅改变可测端口数量”是否会改变可识别上限

### 二、三项工作分别在回答什么问题

#### 1. 小规模主曲线

- 目标：回答“在统一标准协议下，端口数增加后 `K_max` 如何变化”
- 规模只保留：`N = 3, 4, 5, 6`
- 协议保持原始主线：
  - 所有电阻都允许变化
  - 所有边界端口都参与测量
  - 激励仍使用当前标准边界激励协议

#### 2. `6x6` 的可变电阻总数实验

- 目标：回答“真正拉高难度的是每个样本变化了几条边，还是总候选变量数太大”
- 固定：
  - `N = 6`
  - 所有外部端口都可测
  - 激励协议不变
- 改变的是：允许变化的候选电阻集合大小 `M_var`
- 这条线不应通过“删电阻改拓扑”来做，而应通过“全拓扑保留，但只有一部分电阻允许变化，其余永远固定 `R0`”来做

#### 3. `6x6` 的可测端口数实验

- 目标：回答“观测通道数减少时，`K_max` 是否会明显下降”
- 固定：
  - `N = 6`
  - 全部电阻都仍允许变化
  - 电阻变化幅值、数据划分、训练口径不变
- 改变的是：参与测量的边界端口数量 `P_meas`
- 主协议优先解释为“只减少可测端口，不改候选可变电阻集合”

### 三、当前结论对后续路线的影响

- 后续不再把 `MLP` 作为主线 sweep 模型
- 后续主线只保留 `GNN`
- 但不能继续沿 `modelo2_gnn` 的超重架构直接往下推，因为它已经暴露出明显的速度与资源问题
- 因此下一步的核心不是盲目扩规模，而是先做一个更适合中小规模 sweep 的统一 `GNN` 主模型，再进入三项子任务

### 四、执行顺序

当前建议的顺序固定为：

1. 先完成新的统一 `GNN` 中小规模主模型设计
2. 先用这套模型跑子项目 1，拿到 `N=3~6` 的 `P-K_max` 曲线
3. 再在 `6x6` 上做子项目 2，研究 `M_var-K_max`
4. 最后在 `6x6` 上做子项目 3，研究 `P_meas-K_max`

这样安排的原因是：

- 子项目 1 是主线基准，必须先稳定
- 子项目 2 和 3 都属于在 `6x6` 上做控制变量扩展，前提是主线模型和主协议已经固定

## 2026-04-16：子项目 1 优先与 `modelg1` 方案确定

### 一、执行顺序进一步收紧

- 已进一步确认：后续先只推进子项目 1
- 原因很明确：
  - 如果新的统一 `GNN` 在 `6x6` 上主线表现已经明显偏弱
  - 那么继续做 `6x6` 上的子项目 2 和子项目 3，解释价值会很有限
- 因此当前最合理的顺序变为：
  1. 只做子项目 1
  2. 观察 `6x6` 主线结果
  3. 只有当 `6x6` 仍具备解释价值时，才继续推进子项目 2 和 3

### 二、子项目 1 数据策略

- 子项目 1 不重新生成数据
- 直接复用现有正式数据：
  - `N = 3, 4, 5, 6`
  - 固定 `K` 数据集
  - 全边界激励
  - 全边界测量
- 这样做的好处是：
  - 可以把本轮变化来源严格限制在模型结构本身
  - 不会把“模型变化”和“数据协议变化”混在一起

### 三、子项目 3 计划修正

经再次讨论后，子项目 3 的计划已调整为：

- 以后不再定义成“只减少测量端口”
- 改为“减少可用端口集合”，即：
  - 同一组边界端口既负责激励，也负责测量
  - 某个端口如果不在当前可用集合中，就既不能作为激励端口，也不会被测量

这意味着子项目 3 真正研究的是：

- 在同一拓扑下，随着“可用边界端口数”变化，`K_max` 如何变化

### 四、`GNN优化.txt` 的吸收方式

- 已对 `GNN优化.txt` 中的建议进行了吸收整理
- 本轮真正保留的要点包括：
  - 保留多层信息，使用 `JK`
  - 激活函数优先 `GELU / SiLU`
  - 纯回归场景下可考虑轻量稀疏引导
  - 学习率策略可采用余弦退火重启，而不是固定衰减
- 但以下建议暂不纳入第一版 `modelg1`：
  - Graph-NMS 后处理
  - 复杂 ranking loss
  - 额外 `score head`

原因是：

- 子项目 1 的第一目标是先建立一个足够干净、足够稳定、足够快的统一 `GNN` 基线
- 不是一开始就再次把系统复杂化

### 五、当前确定的下一步

- 先按 `modelg1` 方案实现一版四跳中小规模 `GNN`
- 完成后直接用于子项目 1：
  - `N = 3, 4, 5, 6`
  - 复用现有数据
  - 扫 `K`
  - 出新的 `P-K_max` 主图

## 2026-04-16：`modelg1` 子项目 1 首轮结果记录

### 一、结果来源

- 本轮正式汇总基于：
  - 每个输出目录内的 `train_metrics.json`
  - 本地补跑后的 `inference_metrics.json`
- 下载回来的日志文件 `modelg1_subproj1_train.log` 只保留了 `N3x3_K1` 前 31 个 epoch 的片段，未完整覆盖 24 组训练过程
- 因此本轮分析的可靠主依据不是单个 `.log`，而是各 run 自身保存的 `history + best metrics + test inference`

### 二、测试集主结果

按测试集 `id_exact_rate` / `value_accuracy` 统计：

- `N=3, P=8`
  - `K=1: id=0.982, value=0.998`
  - `K=2: id=0.977, value=0.995`
  - `K=3: id=0.953, value=0.994`
  - `K=4: id=0.940, value=0.994`
  - `K=5: id=0.939, value=0.994`
  - `K=6: id=0.926, value=0.994`
- `N=4, P=12`
  - `K=1: id=0.940, value=0.992`
  - `K=2: id=0.901, value=0.991`
  - `K=3: id=0.878, value=0.985`
  - `K=4: id=0.743, value=0.982`
  - `K=5: id=0.724, value=0.980`
  - `K=6: id=0.661, value=0.980`
- `N=5, P=16`
  - `K=1: id=0.957, value=0.995`
  - `K=2: id=0.827, value=0.986`
  - `K=3: id=0.728, value=0.982`
  - `K=4: id=0.579, value=0.976`
  - `K=5: id=0.448, value=0.971`
  - `K=6: id=0.345, value=0.967`
- `N=6, P=20`
  - `K=1: id=0.943, value=0.996`
  - `K=2: id=0.748, value=0.982`
  - `K=3: id=0.553, value=0.975`
  - `K=4: id=0.417, value=0.969`
  - `K=5: id=0.244, value=0.963`
  - `K=6: id=0.179, value=0.952`

### 三、阈值对比结论

保持 `value_accuracy >= 0.90` 不变，仅改变 `id` 通过阈值时，`K_max` 结果为：

- `98%`：`P=8 -> K_max=1`，其余规模为 `0`
- `95%`：`P=8 -> 3`，`P=12 -> 0`，`P=16 -> 1`，`P=20 -> 0`
- `90%`：`P=8 -> 6`，`P=12 -> 2`，`P=16 -> 1`，`P=20 -> 1`
- `85%`：`P=8 -> 6`，`P=12 -> 3`，`P=16 -> 1`，`P=20 -> 1`
- `80%`：`P=8 -> 6`，`P=12 -> 3`，`P=16 -> 2`，`P=20 -> 1`
- `75%`：`P=8 -> 6`，`P=12 -> 3`，`P=16 -> 2`，`P=20 -> 1`

这说明当前 `modelg1` 在数值回归上整体稳定，但一旦考察严格的 exact-support 恢复，`K_max` 会随着规模增大迅速下降。

### 四、训练现象分析

- 本轮最明显的问题不是 `value`，而是 `id`
- 所有规模、所有 `K` 下，`value_accuracy` 基本都维持在 `0.95+`
- 真正的瓶颈是 top-`K` support 排序不稳，尤其在 `N>=5` 后下降明显
- 余弦重启在本轮确实带来了可观测的扰动
  - 例如 `N3x3_K1` 在 epoch `20 -> 21`、`N4x4_K3` 在 epoch `20 -> 21`，`val_id` 都出现明显跳降
  - 多个 run 在 epoch `60 -> 61` 附近也出现同类断崖，这与 warm restart 的重启点一致
- 因此本轮结果支持一个明确判断：
  - `CosineAnnealingWarmRestarts` 对当前“exact ID 优先”的任务并不友好
  - 下一轮更适合改为单调下降学习率，例如 `ReduceLROnPlateau` 或分段衰减

### 五、关于是否收敛

- 大多数 run 在 `best_epoch ≈ 53~60` 附近达到最佳点后早停
- 但也有少数 run 仍在 `epoch=120` 取得最好验证指标，例如：
  - `N4x4_K3`
  - `N4x4_K5`
  - `N6x6_K6`
- 这说明当前策略下不同 `(N, K)` 的训练节奏并不统一
- 原因更像是“调度器与任务不匹配”，而不只是 `max_epoch=120` 单独设小了

### 六、阶段性结论

- `modelg1` 相比前一版超重 `modelo2_gnn`，计算规模更可控、可以顺利完成 `N=3~6` 的一整轮 sweep
- 但当前版本仍没有解决“ID 精确恢复随规模快速退化”的核心问题
- 这轮实验的主要价值是：
  - 明确了 `value` 不是主瓶颈
  - 明确了 `exact ID` 才是主瓶颈
  - 明确了 warm restart 会显著扰动 support 排序
  - 明确了后续模型改动应优先围绕“稳定排序与 support 恢复”来做，而不是继续单纯加大网络

### 七、产物位置

- 测试汇总表：
  - `outputs_modelg1/modelg1_subproj1_test_summary.csv`
- 阈值汇总表：
  - `outputs_modelg1/modelg1_subproj1_threshold_kmax_summary.csv`
- 图像：
  - `Figure/modelg1_subproj1/modelg1_subproj1_test_metric_dropoff_overview.png`
  - `Figure/modelg1_subproj1/N3_P8_test_metric_dropoff.png`
  - `Figure/modelg1_subproj1/N4_P12_test_metric_dropoff.png`
- `Figure/modelg1_subproj1/N5_P16_test_metric_dropoff.png`
- `Figure/modelg1_subproj1/N6_P20_test_metric_dropoff.png`
- `Figure/modelg1_subproj1/modelg1_subproj1_threshold_kmax_compare.png`

## 2026-04-16：`modelg2` 设计与子项目 2/3 规模判断

### 一、为什么要做 `modelg2`

`modelg1` 的首轮结果已经说明：

- 主瓶颈不是数值回归
- 而是 exact-support 排序稳定性

同时训练现象也说明：

- warm restart 会明显扰动 `val_id`
- 小规模图在 5090 上的吞吐仍不理想
- 每个 epoch 都做完整 `train/val` 全量评估，开销偏大

因此下一版优先不改主干结构，而是先做训练协议修正。

### 二、`modelg2` 只做三件事

`modelg2` 基于 `modelg1`，只做：

1. 学习率调度改为单调下降 `ReduceLROnPlateau`
2. `DataLoader` 增加 `num_workers / pin_memory / persistent_workers / prefetch_factor`
3. 完整 `train/val` 评估改为低频执行，不再每个 epoch 全量评估

这意味着：

- 如果 `modelg2` 表现提升，优先归因于训练过程更稳
- 而不是归因于结构更复杂

### 三、对子项目 2 / 3 的规模判断

当前不建议第一轮就直接在 `6x6` 上推进子项目 2 / 3。

原因是：

- 按 `modelg1` 当前结果，`6x6` 的 `K_max` 太低
- 在这种情况下继续扫描 `M_var` 或 `P_active`
- 很可能整条曲线只在很低的 `K` 区间内波动
- 解释空间会比较差

因此当前更合适的第一轮策略是：

- 子项目 1：继续保留 `3x3~6x6`
- 子项目 2：第一轮 pilot 先改在 `4x4`
- 子项目 3：第一轮 pilot 也先改在 `4x4`

`6x6` 暂时更适合放在第二轮做“趋势能否迁移到更大规模”的验证，而不是第一轮主分析规模。

## 2026-04-17：子项目 2 已落地到“可直接开跑”状态

### 一、本次新增内容

围绕子项目 2（固定 `4x4`，改变允许变化的候选电阻总数 `M_var`），已经完成以下落地：

- 新增数据生成脚本：
  - `scripts/generate_subproject2_varcand_grid.py`
- 新增结果汇总与画图脚本：
  - `scripts/summarize_subproject2_varcand.py`
- 调整 `modelg2` 的 `train.py / inference.py`
  - 使其输出自动兼容子项目 2 的数据命名与元信息字段
- 生成了一整套子项目 2 的正式数据：
  - 路径：`data_subproj2_varcand/N4x4/`

### 二、子项目 2 当前数据口径

- 固定拓扑：`4x4`
- 完整电阻网络仍参与基尔霍夫正演求解
- 仅 `candidate_edge_ids` 中的电阻允许变化
- 其余电阻固定为 `R0`
- 全边界端口仍全部参与激励与测量
- 当前扫描矩阵：
  - `M_var ∈ {8, 12, 16, 20, 24}`
  - `K ∈ {1, 2, 3, 4}`
- 每个 `(M_var, K)` 数据量：
  - train `8000`
  - val `1000`
  - test `1000`

### 三、当前完成度

- 正式数据已经全部生成完成
- `modelg2` 已在其中一组数据上完成 `train` 级自检
- 说明当前数据格式与训练代码是打通的
- 下一步直接在云端批量训练即可

### 四、训练后需要产出的两类主图

- 图 1：
  - 固定 `M_var`，横轴为 `K`
  - 纵轴显示 `test_id_exact_rate` 与 `test_value_accuracy`
- 图 2：
  - 固定多个 `ID` 阈值
  - 横轴为 `M_var`
  - 纵轴为 `K_max`

### 五、当前判断

子项目 2 现在不再卡在“方案”阶段，而是已经进入：

- 数据已备好
- 模型已适配
- 汇总脚本已写好
- 只差云端批量训练与回传结果

## 2026-04-17：子项目 2 正式结果与子项目 3 启动

### 一、子项目 2 的正式结果口径

本轮正式结果基于：

- 固定 `4x4`
- 全边界端口激励与测量
- 变量为候选变化电阻总数 `M_var`
- `M_var ∈ {8, 12, 16, 20, 24}`
- `K ∈ {1, 2, 3, 4}`
- 模型统一为 `modelg2`

结果汇总文件：

- `outputs_subproj2_varcand_modelg2/subproject2_varcand_test_summary.csv`
- `outputs_subproj2_varcand_modelg2/subproject2_varcand_threshold_kmax_summary.csv`

新增汇报图：

- `Figure/subproject2_varcand/K1_accuracy_vs_mvar.png`
- `Figure/subproject2_varcand/K2_accuracy_vs_mvar.png`
- `Figure/subproject2_varcand/K3_accuracy_vs_mvar.png`
- `Figure/subproject2_varcand/K4_accuracy_vs_mvar.png`
- `Figure/subproject2_varcand/subproject2_varcand_metric_table.png`

### 二、子项目 2 的主要观察

- `value` 回归精度整体非常稳定，所有组合基本保持在 `0.983~0.997`
- 真正随任务难度快速下降的是 `id exact rate`
- 当 `M_var` 从 `8` 增加到 `24` 时，`K=3/4` 的 `id` 精度下降最明显
- 这说明任务难度的主来源不是数值回归，而是候选组合空间膨胀后对 support 排序的干扰

按阈值看：

- 在 `98%` 的 `ID` 阈值下：
  - `M_var=8` 时 `K_max=2`
  - `M_var=12` 时 `K_max=1`
  - `M_var>=16` 时没有组合通过
- 在 `95%` 的 `ID` 阈值下：
  - `K_max` 依次为 `4, 4, 2, 1, 1`

### 三、对子项目 2 的一句话结论

在 `4x4` 全端口条件下，随着允许变化的候选电阻数 `M_var` 增大，`value` 回归几乎不变而 `id` 识别显著下降，说明当前识别上限主要受组合搜索难度而不是数值回归误差限制。

### 四、子项目 3 已启动的实现工作

子项目 3 当前已进入“代码与数据协议已准备”状态：

- 已扩展公共数据接口，使其支持“活动端口子集”
- 已让 `modelg2` 自动兼容 `4` 通道与 `5` 通道输入
- 新增脚本：
  - `scripts/generate_subproject3_activeport_grid.py`
  - `scripts/summarize_subproject3_activeport.py`

当前子项目 3 的协议为：

- 第一轮 pilot 固定 `4x4`
- 扫描 `P_active ∈ {4, 6, 8, 10, 12}`
- 扫描 `K ∈ {1, 2, 3, 4}`
- 全部电阻都允许变化
- 同一组活动端口同时负责激励与测量

烟雾验证已经确认：

- 数据可成功生成
- `load_split_from_meta` 输出已变为 `(..., num_nodes, 5)` 结构
- 第五维即 `active_port_mask`

## 2026-04-17：子项目 3 正式结果回收

### 一、结果范围

本轮子项目 3 基于：

- 固定 `4x4`
- 全部电阻都允许变化
- 变量为活动端口数 `P_active`
- `P_active ∈ {4, 6, 8, 10, 12}`
- `K ∈ {1, 2, 3, 4}`
- 同一组活动端口同时负责激励与测量
- 模型统一为 `modelg2`

结果汇总文件：

- `outputs_subproj3_activeport_modelg2/subproject3_activeport_test_summary.csv`
- `outputs_subproj3_activeport_modelg2/subproject3_activeport_threshold_kmax_summary.csv`

新增汇报图：

- `Figure/subproject3_activeport/K1_accuracy_vs_pactive.png`
- `Figure/subproject3_activeport/K2_accuracy_vs_pactive.png`
- `Figure/subproject3_activeport/K3_accuracy_vs_pactive.png`
- `Figure/subproject3_activeport/K4_accuracy_vs_pactive.png`
- `Figure/subproject3_activeport/subproject3_activeport_metric_table.png`

### 二、主要观察

- 随着 `P_active` 增大，`ID` 精度总体显著提升
- `value` 回归也提升，但幅度明显小于 `ID`
- 真正最敏感的是 `K=2/3/4` 的 support 识别

具体测试趋势：

- `K=1`：
  - 从 `P_active=8` 开始基本进入平台区，约 `0.96~0.97`
- `K=2`：
  - 从 `0.526`（4 端口）提升到 `0.903`（12 端口）
- `K=3`：
  - 从 `0.051`（4 端口）提升到 `0.841`（12 端口）
- `K=4`：
  - 从 `0.007`（4 端口）提升到 `0.713`（12 端口）

这说明：

- 端口数减少会首先压垮多电阻同时变化时的 `ID` 重建能力
- 多端口测量对可辨识性不是可有可无，而是核心决定因素之一

### 三、如何解读局部非单调

本轮曲线虽然总体随 `P_active` 增大而上升，但也存在局部不严格单调的点。

这类现象当前不应过度解读为“更多端口反而更差”，更合理的解释是：

- 当前每个 `P_active` 只取了一组固定的均匀子集
- 不同端口子集的空间几何布局并不等价
- 因而当前结果同时混合了：
  - 端口数量效应
  - 端口位置几何效应

所以当前子项目 3 更适合得出：

- “总体趋势上，更多活动端口显著提升可识别上限”

而不适合得出：

- “每增加两个端口都严格单调提升”

若后续要把这件事做得更严谨，应在同一 `P_active` 下增加多组端口子集重复实验，再比较平均值和方差。

### 四、关于 `120 epoch` 是否偏少

本轮共 `20` 组任务，其中 `8` 组的 `best_epoch = 120`，说明至少有一部分组合在训练结束时仍未完全进入稳定平台。

因此当前更稳妥的判断是：

- `120 epoch` 对当前任务可用于先看趋势
- 但若要进一步逼近最终上限，后续可把 `max_epoch` 提高到 `160~180`
- 同时保留 `ReduceLROnPlateau + early stopping`，避免无效拖长

换句话说：

- 当前结论已经足够说明“端口数确实重要”
- 但具体极限值仍可能因训练轮数增加而继续小幅上升

### 五、对子项目 3 的一句话结论

在固定 `4x4`、全边可变的条件下，减少活动端口数会显著压低多电阻同时变化时的 `ID` 识别能力，说明多端口循环激励/测量确实在提供有效的可辨识信息，而不仅仅是数值冗余。

## 2026-04-17：子项目 4 已进入“可直接开跑”状态

### 一、立项目的

在子项目 3 中，我们已经看到“可用端口数减少”会显著压低 `ID` 识别能力。  
但那条结果仍然同时混合了两个因素：

- 端口数量本身变少
- 激励与测量集合同时变少

因此需要单独再做一条更受控的实验线，只改“激励次数”而不改“测量端口数”，更直接判断：

- 多端口循环激励是否真的增加了有效信息通道
- 还是主要在提高数值稳定性

### 二、当前固定口径

- 拓扑固定：`4x4`
- 候选变化边：全边集合
- 测量端口：固定为全部 `12` 个边界端口
- 研究变量：激励次数 `E`
- 第一轮比较集合：
  - `E ∈ {1, 4, 12}`
- 变化数量扫描：
  - `K ∈ {1, 2, 3, 4, 5, 6}`
- 数据规模：
  - train `8000`
  - val `1000`
  - test `1000`

### 三、已完成的实现

已新增数据生成脚本：

- `scripts/generate_subproject4_excitation_grid.py`

其特点是：

- 对每个 `(E, K)` 单独生成独立数据集
- 仍然直接解基尔霍夫方程做正演
- 测量端口始终保留完整 `12` 个边界节点
- 只通过 `excitation_count` 压缩激励集合

已新增汇总脚本：

- `scripts/summarize_subproject4_excitation.py`

其输出约定已固定为：

- `outputs_subproj4_excitation_modelg2/subproject4_excitation_test_summary.csv`
- `outputs_subproj4_excitation_modelg2/subproject4_excitation_threshold_kmax_summary.csv`
- `outputs_subproj4_excitation_modelg2/subproject4_excitation_metric_table.csv`
- `Figure/subproject4_excitation/subproject4_excitation_curve_compare.png`
- `Figure/subproject4_excitation/subproject4_excitation_threshold_kmax_compare.png`
- `Figure/subproject4_excitation/subproject4_excitation_metric_table.png`
- `Figure/subproject4_excitation/K1_accuracy_vs_excitation.png` 到 `K6_accuracy_vs_excitation.png`

### 四、为什么这条实验线更合理

这条子项目 4 相比只看单一 `K_max`，有两个优势：

- 它先比较整条 `K-id_exact_rate` 曲线，不会过早被某一个阈值绑死
- 它把“测量端口数”固定下来，只看“激励多样性”本身的作用

因此如果后面结果表现为：

- `E` 增加后整条 `ID` 曲线整体上移
  - 更支持“激励次数确实增加了有效可辨识信息”
- `K_max` 变化不大，但 `value` 或中低 `K` 的稳定性变好
  - 更支持“激励主要在提高稳定性”

### 五、当前下一步

- 正式生成 `E ∈ {1, 4, 12}`、`K ∈ {1..6}` 的数据
- 复用 `modelg2` 完成训练与测试
- 汇总三类主图与一张测试集数值表

## 2026-04-18：子项目 4 正式结果回收

### 一、实验范围

本轮子项目 4 固定如下：

- 拓扑：`4x4`
- 全边可变：`M = 24`
- 测量端口固定为全部 `12` 个边界端口
- 激励次数：`E ∈ {1, 4, 12}`
- 变化电阻数量：`K ∈ {1, 2, 3, 4, 5, 6}`
- 模型：`modelg2`
- 训练上限：`160 epoch`

### 二、测试集关键结果

测试集 `ID / Value` 结果如下：

- `E=1`
  - `K=1`: `0.882 / 0.992`
  - `K=2`: `0.464 / 0.950`
  - `K=3`: `0.150 / 0.903`
  - `K=4`: `0.055 / 0.849`
  - `K=5`: `0.029 / 0.829`
  - `K=6`: `0.006 / 0.802`
- `E=4`
  - `K=1`: `0.976 / 0.993`
  - `K=2`: `0.912 / 0.990`
  - `K=3`: `0.805 / 0.977`
  - `K=4`: `0.658 / 0.973`
  - `K=5`: `0.601 / 0.971`
  - `K=6`: `0.540 / 0.968`
- `E=12`
  - `K=1`: `0.964 / 0.990`
  - `K=2`: `0.911 / 0.993`
  - `K=3`: `0.863 / 0.986`
  - `K=4`: `0.773 / 0.982`
  - `K=5`: `0.727 / 0.982`
  - `K=6`: `0.654 / 0.980`

### 三、阈值侧结论

在 `ID` 阈值分别取 `95% / 90% / 85% / 80%` 时，`K_max` 为：

- `E=1`
  - `95%`: `0`
  - `90%`: `0`
  - `85%`: `1`
  - `80%`: `1`
- `E=4`
  - `95%`: `1`
  - `90%`: `2`
  - `85%`: `2`
  - `80%`: `3`
- `E=12`
  - `95%`: `1`
  - `90%`: `2`
  - `85%`: `3`
  - `80%`: `3`

### 四、训练过程观察

- `E=1` 在 `K>=3` 时很早就进入失败区，`best_epoch` 依次只有 `120 / 78 / 74 / 62`
- `E=4` 与 `E=12` 的大多数任务都能训练到接近上限，说明在多激励条件下，模型并没有过早“学不动”，而是在继续挖掘更稳定的区分信息
- `Value` 回归在 `E=4` 和 `E=12` 下始终较高，真正拉开差距的仍然是 `ID exact rate`

### 五、当前结论

- 单次激励不足以支撑多电阻同时变化场景，`K` 一旦从 `1` 增加到 `2` 以上，`ID` 精度会快速塌陷
- 从 `E=1` 增加到 `E=4` 带来的是实质性跃迁，说明多激励并不只是数值冗余，而是在显著增加有效可辨识信息
- 从 `E=4` 增加到 `E=12` 后，低 `K` 区域提升有限，但在 `K>=3` 时仍有稳定增益，说明更多激励主要体现在复杂场景下的信息补充
- 因此，子项目 4 的结果支持这样一个判断：循环多端口激励确实提供了有效信息通道，且其价值主要体现在高复杂度重建任务中

### 六、图表与汇总修正

- 已修正子项目 4 汇总脚本中 `E` 的读取口径，避免先前汇总表中错误显示为 `E=0`
- 已重新生成以下正式汇报图：
  - `Figure/subproject4_excitation/subproject4_excitation_accuracy_by_k_overview.png`
  - `Figure/subproject4_excitation/subproject4_excitation_threshold_kmax_compare.png`
  - `Figure/subproject4_excitation/subproject4_excitation_metric_table.png`
- 额外补充了两类辅助图：
  - 结果归一化图：`Figure/subproject4_excitation/subproject4_excitation_kmax_per_excitation.png`
  - 结构负载图：`Figure/subproject4_excitation/subproject4_resistors_per_excitation.png`

## 2026-04-19：情况 A 与情况 B 结果回收

### 一、数据生成规则补记

以下规则适用于本轮情况 A（`N=7,8`）与情况 B（`N=3` 深挖）：

- 拓扑固定为正方形电阻网络 `N x N`
- 电阻边总数为 `M = 2N(N-1)`
- 边界端口数为 `P = 4N - 4`
- 每个样本固定恰好有 `K` 条变化电阻
- 变化位置从全部 `M` 条电阻边中随机抽取，样本内不重复
- 基准电阻固定为 `R0 = 1000 Ω`
- 变化电阻值按连续均匀分布采样：`R = R0 * (1 + δ)`，其中 `δ ∈ [-0.2, 0) ∪ (0, 0.2]`
- 无测量噪声
- 仍然使用完整边界循环激励集合；若未显式指定 `excitation_count`，则默认采用全部边界相邻端口顺序激励
- 正演求解方式为：直接构建电导矩阵，按节点电压法求解基尔霍夫方程，得到边界电压响应
- 数据划分固定为：
  - train `8000`
  - val `1000`
  - test `1000`

### 二、情况 A：任务 1 扩展到 `7x7`、`8x8`

#### 目的

- 在不修改模型的前提下，将任务 1 扩展到更大规模，观察 `modelg2` 在 `N=7,8` 下的可识别性是否继续下降。

#### 实践

- 固定 `modelg2` 架构、训练协议与数据口径不变，只补充 `N=7x7`、`8x8`，并仅测试 `K = 1,2,3,4` 四个变化数量。

#### 测试集结果

- `N=7`
  - `K=1`: `ID=0.942`, `Value=0.995`
  - `K=2`: `ID=0.743`, `Value=0.974`
  - `K=3`: `ID=0.460`, `Value=0.968`
  - `K=4`: `ID=0.375`, `Value=0.958`
- `N=8`
  - `K=1`: `ID=0.912`, `Value=0.986`
  - `K=2`: `ID=0.226`, `Value=0.916`
  - `K=3`: `ID=0.167`, `Value=0.840`
  - `K=4`: `ID=0.068`, `Value=0.839`

#### 训练侧观察

- `N=7` 仍能把训练集 `ID` 推到较高水平：
  - `K=1`: `train_id=0.941`
  - `K=2`: `0.764`
  - `K=3`: `0.535`
  - `K=4`: `0.491`
- `N=8` 从 `K=2` 开始训练集 `ID` 就显著塌陷：
  - `K=1`: `train_id=0.932`
  - `K=2`: `0.235`
  - `K=3`: `0.159`
  - `K=4`: `0.064`
- train-test gap 很小，尤其 `N=8` 基本接近于零，说明主要瓶颈不是泛化，而是训练集本身就难以精确恢复 support。

#### 当前结论

- 随规模继续增大，`Value` 回归仍可维持较高，但 `ID` 识别快速恶化。
- `N=7` 尚能较稳定处理 `K=1`，但 `K>=2` 已明显下降；`N=8` 从 `K=2` 起几乎进入失效区。
- 因而当前主线瓶颈仍是“变化位置识别”而非“变化幅值回归”，且其中包含明显的问题本身可解性上限。

### 三、情况 B：`N=3` 全部潜力深挖

#### 目的

- 针对当前表现最好的 `N=3`，继续扩展更大的变化数量，观察在较宽松阈值下最多能达到多大的原始变化数量。

#### 实践

- 在已有 `K=1..6` 基础上继续补充 `K=7..11`，保持 `modelg2`、数据协议和评估方式不变。

#### 测试集结果

- `K=1..6`
  - `0.984 / 0.996`
  - `0.980 / 0.996`
  - `0.963 / 0.995`
  - `0.951 / 0.994`
  - `0.931 / 0.993`
  - `0.933 / 0.993`
- `K=7..11`
  - `K=7`: `0.959 / 0.996`
  - `K=8`: `0.956 / 0.996`
  - `K=9`: `0.948 / 0.993`
  - `K=10`: `0.946 / 0.996`
  - `K=11`: `0.962 / 0.995`

#### 阈值侧结果

- `98%` 阈值：`K_max = 2`
- `95%` 及以下阈值：`K_max = 11`

#### 结果解释

- 从原始变化数量 `K` 看，`N=3` 在 `95% / 90% / 85% / 80% / 75%` 阈值下都可以达到 `K=11`。
- 但这并不意味着难度随 `K` 单调增加；当 `K > 6` 时，问题逐渐转化为“只有少数几条边不变”，其组合难度与前半段并不等价。
- 因而 `N=3` 的结论需要分两层理解：
  - 从“原始变化数量”口径看，模型几乎可覆盖全部变化数区间
  - 从“稀疏识别难度”口径看，最难区域仍然集中在中间段，而不是最大的原始 `K`

#### 当前结论

- `N=3` 在小规模下具有极强恢复能力，低阈值下原始 `K_max` 已可达到 `11`
- 但该结果带有明显的“补集效应”，不能简单外推为“大规模下也会同样成立”
- 因而 `N=3` 更适合作为“能力上限样例”和“问题结构分析样例”，而不宜直接代表一般规模下的真实难度

## 2026-04-19：新增方向立项：深度对问题难度的影响

### 目的

- 在固定拓扑和固定边界观测协议下，判断“靠近边界的电阻是否比靠近内部的电阻更容易被识别”。

### 当前决定

- 先选 `4x4` 与 `5x5` 两种规模作为首轮验证对象
- 先固定 `K = 1`，避免多变化组合干扰“深度效应”的判断
- 激励与测量协议保持不变：
  - 只使用边界节点作为激励端口
  - 只使用边界节点作为测量节点
- 模型先固定为当前统一参考模型 `modelg2`

### 技术定义

- 节点深度定义为其到边界的最小网格步数
- 电阻边深度定义为其两个端点深度的较小值
- 在 `4x4`、`5x5` 上，首轮主要比较“外层电阻”和“内层电阻”

### 执行策略

- 先做一轮“快速验证”：
  - 直接在现有 `4x4/5x5, K=1` 结果上按深度分层统计
- 若趋势明显，再进入“正式平衡采样实验”：
  - 重新生成按深度均衡采样的数据
  - 排除外层样本数量天然更多带来的统计偏差

### 当前预期结论口径

- 若外层电阻的 `ID` 和 `Value` 都稳定高于内层，则说明边界邻近性确实提高了可辨识性
- 若 `5x5` 中内外差异大于 `4x4`，则说明随规模增大，深度带来的难度惩罚会增强
- 若差异不显著，则说明当前瓶颈可能更多来自候选边总数或结构相似性，而不只是深度本身

## 2026-04-19：子项目 5 已进入“可直接开跑”状态

### 一、已完成的落地工作

- 在 `project_common.py` 中补充了统一的边深度定义工具：
  - 节点到边界的最小步数
  - 电阻边深度 `depth(edge) = min(depth(u), depth(v))`
- 新增 `generate_subproject5_depth_grid.py`
  - 用于生成 `4x4/5x5` 的深度平衡数据
  - 当前正式口径固定为 `K=1`
  - train / val / test 会在不同深度层之间做平衡采样
- 新增 `summarize_subproject5_depth.py`
  - 既能读取后续正式平衡采样版输出
  - 也能直接读取现有任务 1 的 `4x4/5x5, K=1` 输出做快速统计

### 二、现有结果上的快速验证

在不重新训练、直接使用任务 1 现有 `modelg2` 输出的情况下，对 `4x4` 和 `5x5` 的 `K=1` 测试集做了按深度分层统计：

- `4x4`
  - 外层：`ID = 0.957`
  - 内层：`ID = 0.948`
- `5x5`
  - 外层：`ID = 0.956`
  - 内层：`ID = 0.990`

### 三、当前判断

- 现有随机采样数据上，`4x4` 与 `5x5` 并没有表现出一致的“外层一定更容易”规律
- 这并不意味着深度无效，更可能说明：
  - 当前样本按全体电阻随机采样，外层与内层样本比例不平衡
  - 单次快速统计仍混有样本分布偏差
- 因而继续推进“深度平衡采样正式版”是必要的，后续结论应以正式版结果为准

## 2026-04-19：子项目 5 正式平衡采样版结果回收

### 一、实验设置

- 拓扑固定为：
  - `4x4`
  - `5x5`
- 每个样本固定：
  - `K = 1`
- 激励与测量仍只使用边界节点
- 数据改为按深度层平衡采样：
  - `outer`
  - `inner_d1`
- 模型继续使用：
  - `modelg2`

### 二、整体测试结果

- `4x4`
  - 全体测试集：`ID = 0.973`，`Value = 0.994`
- `5x5`
  - 全体测试集：`ID = 0.976`，`Value = 0.990`

### 三、按深度层拆分结果

- `4x4`
  - `outer`: `ID = 0.962`, `Value = 0.9966`
  - `inner_d1`: `ID = 0.984`, `Value = 0.9915`
- `5x5`
  - `outer`: `ID = 0.966`, `Value = 0.9929`
  - `inner_d1`: `ID = 0.986`, `Value = 0.9868`

### 四、当前观察

- 在这轮正式平衡采样实验中，内层电阻的 `ID exact rate` 反而 consistently 高于外层电阻
- 但与此同时，内层电阻的 `Value accuracy` 反而 consistently 低于外层电阻
- 因此“靠边界更容易识别”并没有直接成立，当前更像是出现了：
  - 内层位置更容易判对
  - 但内层幅值更难回归准

### 五、当前解释口径

当前结果不能直接下结论说“内层一定更容易”，原因有两点：

- 本轮只是按深度层平衡采样，还不是按具体边逐条平衡采样
- 外层候选边数量仍明显多于内层：
  - `4x4`: outer `20` 条，inner `4` 条
  - `5x5`: outer `28` 条，inner `12` 条

这意味着：

- 虽然两个深度层的样本数相同
- 但平均到每一条具体电阻边上的训练样本数，内层仍更多

### 六、当前结论

- 这轮结果已经说明：深度效应不是简单的“越靠外越容易”
- 在当前协议下，`ID` 与 `Value` 可能反映的是两种不同难度：
  - 位置可分性
  - 幅值可回归性
- 下一步若要进一步确认“深度”本身的作用，应继续做：
  - 按具体边再平衡一次样本数
  - 或补充单边灵敏度/响应强度分析，与学习结果交叉验证
# 2026-04-28 电阻组合可辨识性子项目初始化

- 新增 `combo_identifiability/` 子项目。
- 目标：研究固定 `N`、固定候选池 `C`、固定实际变化电阻数 `R >= 2` 时，不同电阻组合的识别难度是否不同。
- 初版实现：新增 `scripts/analyze_pair_sensitivity.py`，用于枚举两电阻组合并计算物理灵敏度、响应相似度、组合响应强度和 easy score。
- 后续计划：先用物理灵敏度筛选 easy / hard 组合，再生成对应平衡测试集，用同一个模型验证预测准确率差异。
# 2026-05-06 理论框架验证子项目

- 新增 `theory_validation/`，用于验证候选电阻响应矩阵秩 / SVD 是否能够解释和指导 `K_max`。
- 已完成本地理论分析脚本：
  - `theory_validation/analyze_candidate_pools.py`
  - 输出候选池边编号：`theory_validation/outputs/candidate_pools.json`
  - 输出理论指标表：`theory_validation/outputs/candidate_pool_metrics.csv`
  - 输出候选池拓扑图、奇异值谱图和理论指标对比图。
- 当前本地分析固定在 `4x4`，比较随机、外侧优先、内侧优先、低相似度优先、SVD 贪心优化五类候选池。
- 后续云端验证：使用 `generate_theory_validation_data.py` 按候选池策略生成数据，再统一使用 `modelg2` 训练。

## 2026-05-06 理论框架验证训练完成

- 已完成 `theory_validation` 的第一批云端训练结果整理。
- 实验对象：`4x4` 正方形网络，`C=16/24`，`K=1..4`，共 `32` 组训练。
- 关键结论：在 `C=16` 时，低相似度候选池和 `SVD` 优化候选池显著优于随机候选池。
  - 随机候选池平均 `ID` 精度：`0.855`，`K=4` 时 `0.733`
  - 低相似度候选池平均 `ID` 精度：`0.914`，`K=4` 时 `0.875`
- `SVD` 优化候选池平均 `ID` 精度：`0.915`，`K=4` 时 `0.862`
- 解释：理论指标更好的候选池具有更高稳定秩、更低最大响应相似度，因此更容易被同一 `modelg2` 重构。
- 论文意义：该结果把前面观察性实验推进到“可用于指导器件制造候选编码区域选择”的半定量理论框架。

# 2026-05-11 理论验证补充：纯数学白箱反演

- 在 `theory_validation/` 中新增 `math_reconstruction.py`。
- 这条新链路不再依赖神经网络，而是直接复用当前的正演物理模型和 Jacobian：
  - 固定候选池整体看 `rank / sigma_min / condition_number`
  - 固定 `K` 时，对所有 `K` 子集做组合枚举
  - 每个组合内部直接解最小二乘，再按残差最小选位置
- 这使得老师的“固定候选集合判秩”方法被补成了完整白箱流程：
  - 若位置已知：直接解数值
  - 若位置未知但 `K` 已知：组合搜索 + 线性重构
- 本地 `4x4` 小样本探针表明：
  - `C=12` 下 `low_similarity / svd` 候选池的 `K=1..4` 子集条件数几乎理想，白箱搜索可稳定精确恢复
  - `C=16` 下低相似度候选池的子集条件数仍明显优于随机候选池，对应的白箱重构也更稳定
- 当前判断：
  - “候选池整体满秩”只回答固定位置集合是否理论可解
  - 真正的未知位置恢复还要进一步看 `K` 子集层级的奇异值、条件数和残差间隔
