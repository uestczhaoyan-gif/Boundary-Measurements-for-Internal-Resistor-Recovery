# 电阻组合可辨识性实验记录

## 2026-04-28 初始化

- 新建 `combo_identifiability/` 子项目。
- 明确研究目标：固定规模、固定候选池、固定 `R >= 2` 时，研究不同变化电阻组合的可识别性差异。
- 初版优先实现 `R = 2` 的组合物理灵敏度分析。
- 第一阶段不重新训练模型，先用基尔霍夫正向求解得到单边电压响应灵敏度，并据此对两电阻组合进行排序。

下一步：

- 运行 `scripts/analyze_pair_sensitivity.py` 得到 `N=4, C=24, R=2` 的组合难度表和图。
- 选取 easy / hard 组合，生成分组测试集。
- 用同一个模型验证 easy / hard 组合是否表现出显著识别精度差异。

